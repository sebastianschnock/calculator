/**
 * Converts a string in infix notation to Reverse Polish notation (RPN).
 * eg: "1 + 2" would be converted to "1 2 +"
 * 
 * @method     convertInfixToRPN
 * @param      {String}  infix   { the string in infix to convert }
 */
function convertInfixToRPN(infix) {
	return '';
}

module.exports = { convertInfixToRPN };